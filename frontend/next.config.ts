import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['encrypted-tbn0.gstatic.com', 'pbs.twimg.com', 'i.pinimg.com'],
  },
};

export default nextConfig;
